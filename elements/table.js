/* jshint newcap: false */
Polymer('qowt-table', {
  /* jshint newcap: true */

  // ---------------------- PRIVATE ------------------

});

QowtElement.addMixin(QowtTable.prototype);
FlowChildren.addMixin(QowtTable.prototype);